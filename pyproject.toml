[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "livekit-api>=1.0.2",
    "livekit>=1.0.6",
    "streamlit>=1.45.0",
]
